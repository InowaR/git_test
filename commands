git init
git status
git diff <имя файла>
git add -A
git commit -m "init commit"
git log
git branch <имя ветки>
git checkout -b <имя ветки>
git revert <номер коммита> - создается новый revert коммит без изменений плохого коммита
git restore <имя файла> - восстановление проекта до коммита (кнопка отменить)
git reset --soft <номер коммита> - перемещает указатель HEAD, файлы остаются
git reset --mixed <номер коммита> - перемещает указатель HEAD и убирает файлы из коммита
git reset --hard <номер коммита> - перемещает укзатель HEAD, удаляет файлы
